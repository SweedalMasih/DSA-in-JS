class Box {
  constructor(height, width) {
    this.height = height;
    this.width = width;
  }

  calcArea() {
    return this.height * this.width;
  }
}

const square = new Box(10, 10);
const rectangle = new Box(10, 20);

console.log(square.calcArea());
console.log(rectangle.calcArea());
